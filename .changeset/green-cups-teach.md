---
"@devsym/graph-toolkit-react": patch
---

Add Storybook autodocs coverage for `PeoplePicker` and document the component story pattern so new components use a single Docs page that summarizes their feature stories.
